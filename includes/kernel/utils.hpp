#pragma once 

#define sa(i) sa[i]
#define sx(i) sx[i]
#define sy(i) sy[i]

#define da(i) da[i]
#define dx(i) dx[i]
#define dy(i) dy[i]

#define sA(i,j) sA[j * P + i]
#define sB(i,j) sB[j * N + i]
#define sC(i,j) sC[j * N + i]

